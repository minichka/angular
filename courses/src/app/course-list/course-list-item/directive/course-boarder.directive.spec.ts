import { CourseBoarderDirective } from './course-boarder.directive';

describe('CourseBoarderDirective', () => {
  it('should create an instance', () => {
    // const directive = new CourseBoarderDirective();
    // expect(directive).toBeTruthy();
  });
});
